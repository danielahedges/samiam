extends layout

block content
	if user
		div.container
			section(ng-view)
	else
		// No user logged in.
		div.container
			include public_validationForm
		div.container
			include messages
			include loginForm
